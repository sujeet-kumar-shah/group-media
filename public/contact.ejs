<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <link rel="shortcut icon" href="favicon.ico" type="image/ico" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="assets/css/styles.css?v=1.1.4" />
  <title>Contact Us - Social Media Manager</title>
  <style>
    /* Base styles */
    body {
      font-family: var(--bs-body-font-family);
      font-size: var(--bs-body-font-size);
      font-weight: var(--bs-body-font-weight);
      line-height: var(--bs-body-line-height);
      color: var(--bs-body-color);
      background: var(--bs-body-bg);
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Navbar styles */
    .navbar {
      background-color: var(--bs-white);
      padding: 1rem 0;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .navbar-brand img {
      height: 40px;
    }

    .navbar-toggler {
      border: none;
      outline: none;
    }

    .navbar-toggler .icon-bar {
      width: 22px;
      height: 2px;
      background-color: var(--bs-body-color);
      display: block;
      margin: 4px auto;
      transition: all 0.2s;
    }

    .navbar-nav {
      margin-left: auto;
    }

    .nav-link {
      color: var(--bs-body-color);
      padding: 0.5rem 1rem;
      text-transform: uppercase;
      font-weight: 500;
      transition: color 0.2s;
    }

    .nav-link:hover {
      color: var(--bs-primary);
    }

    .navbar-socials img {
      height: 24px;
      margin: 0 8px;
    }

    .btn-main {
      background-color: var(--bs-primary);
      color: var(--bs-white);
      border: none;
      padding: 0.5rem 1rem;
      text-transform: uppercase;
      font-weight: 500;
      transition: background-color 0.2s;
    }

    .btn-main:hover {
      background-color: var(--bs-primary-rgb);
    }

    /* Banner section */
    .banner {
      background-color: var(--bs-white);
      padding: 3rem 0;
      text-align: center;
    }

    .banner img {
      max-width: 100%;
      height: auto;
    }

    .banner h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--bs-primary);
    }

    .banner p {
      font-size: 1.1rem;
      color: var(--bs-body-color);
      margin-bottom: 2rem;
    }

    /* Contact form styles */
    .contact-form {
      padding: 3rem 0;
      background-color: var(--bs-light);
    }

    .form-container {
      max-width: 60%;
      margin: 0 auto;
      padding: 2rem;
      background-color: var(--bs-white);
      border-radius: 10px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    }

    .contact-form h2 {
      text-align: center;
      margin-bottom: 2rem;
      font-size: 2rem;
      color: var(--bs-primary);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-container label {
      display: block;
      font-weight: bold;
      color: var(--bs-primary);
      margin-bottom: 0.5rem;
    }

    .form-container input,
    textarea {
      width: 100%;
      padding: 1rem;
      border: 1px solid var(--bs-light);
      border-radius: 4px;
      margin-bottom: 1rem;
      resize: vertical;
    }

    .submit-button {
      padding: 1rem 2rem;
      background-color: var(--bs-primary);
      border: none;
      color: var(--bs-white);
      border-radius: 4px;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .submit-button:hover {
      background-color: var(--bs-primary-rgb);
    }

    /* Contact info styles */
    .contact-info {
      text-align: center;
      padding: 3rem 0;
      background-color: var(--bs-light);
      color: var(--bs-body-color);
    }

    .contact-info h2 {
      margin-bottom: 2rem;
      font-size: 2rem;
      color: var(--bs-primary);
    }

    .contact-info p {
      font-size: 1.1rem;
      line-height: 1.8;
    }

    /* Footer styles */
    footer {
      background-color: var(--bs-dark);
      color: var(--bs-white);
      padding: 1.5rem 0;
      text-align: center;
    }

    footer p {
      margin: 0;
      font-size: 0.875rem;
    }

  </style>
</head>

<body>
  <header>
    <nav class="navbar navbar-expand-lg">
      <div class="container">
        <a class="navbar-brand" href="/"><img src="public/Screenshot 2024-07-09 133947.jpg" alt="Logo" /></a>
        <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
          aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/login">login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/contact">Contact</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="https://portfolio-mu-blush-10.vercel.app/">About</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <!-- Banner section -->
  <section class="banner">
    <div class="container">
      <img src="https://media.geeksforgeeks.org/wp-content/uploads/20230822131732/images.png" alt="Welcome to our Contact Us page">
      <h1>Get in Touch With Us</h1>
      <p>We're here to answer any questions you may have.</p>
    </div>
  </section>

  <!-- Contact form -->
  <section class="contact-form">
    <div class="container">
      <div class="form-container">
        <h2>Your Details</h2>
        <form action="#" method="POST">
          <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" required>
          </div>

          <div class="form-group">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required>
          </div>

          <div class="form-group">
            <label for="phone">Phone:</label>
            <input type="tel" id="phone" name="phone">
          </div>

          <div class="form-group">
            <label for="message">Message:</label>
            <textarea id="message" name="message" rows="4" required></textarea>
          </div>

          <button type="submit" class="submit-button">Submit</button>
        </form>
      </div>
    </div>
  </section>

  <!-- Contact info -->
  <section class="contact-info">
    <div class="container">
      <h2>Contact Information</h2>
      <p>Email: <a href="mailto:sujeetshah321s@gmail.com">sujeetshah321s@gmail.com</a></p>
      <p><a href="https://github.com/sujeet-kumar-shah/media-group">Github</a></p>
    </div>
  </section>

  <!-- Footer section -->
  <footer>
    <div class="container">
      <p>&copy; 2024 All rights reserved.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function openNavbar() {
      const navMenu = document.getElementById("nav-menu");
      navMenu.classList.toggle("active");
    }
  </script>
</body>

</html>
